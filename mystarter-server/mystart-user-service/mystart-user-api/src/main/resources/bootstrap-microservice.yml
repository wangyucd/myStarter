# 用户模块 - 微服务模式 Nacos 配置
spring:
  application:
    name: mystarter-user
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: mystarter
        file-extension: yml
        group: DEFAULT_GROUP
      discovery:
        server-addr: localhost:8848
        namespace: mystarter
